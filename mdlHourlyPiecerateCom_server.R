
mdlHourlyPiecerateServer::HourlyPieceServer(input = input,output = output,session = session,dms_token = dms_token)
mdlHourlyPiecerateServer::HourlyWagesServer(input = input,output = output,session = session,dms_token = dms_token)


mdlHourlyPiecerateServer::HourlyRoutingServer(input = input,output = output,session = session,dms_token = dms_token)

mdlHourlyPiecerateServer::productRoutingServer(input = input,output = output,session = session,dms_token = dms_token)
mdlHourlyPiecerateServer::productpriceServer(input = input,output = output,session = session,dms_token = dms_token)
mdlHourlyPiecerateServer::productdailyServer(input = input,output = output,session = session,dms_token = dms_token,erp_token=erp_token)

mdlHourlyPiecerateServer::HandywagesServer(input = input,output = output,session = session,dms_token = dms_token)
#完成日期组件注册

